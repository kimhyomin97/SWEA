// BOJ 6549 히스토그램에서 가장 큰 직사각형 segment tree
#include <iostream>
#define size (1 << 21)
using namespace std;

int main(){
	ios::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	int n;
	while(true){
		cin >> n;
		if(n == 0) break;
		int list[n];
		for(int i=0; i<n; i++){
			cin >> list[i];
		}
	}
	
	return 0;
} 
